const fs = require('fs'); let content = fs.readFileSync('src/lib/prompts.ts', 'utf8'); const extractLines = content.split('
'); const startIdx = extractLines.findIndex(l => l.includes('САМОЕ ВАЖНОЕ ПРАВИЛО СТИЛЯ')); let endIdx = extractLines.findIndex(l => l.includes('Соблюдай строгий запрет на раннее раскрытие статуса героя.')); if(startIdx !== -1 && endIdx !== -1) { const avatarEnd = extractLines.findIndex((l, idx) => idx > startIdx && l === 'Если Avatar не указан для этой части — не добавляй его.'); if (avatarEnd !== -1) { endIdx = avatarEnd; } extractLines.splice(startIdx, endIdx - startIdx, fs.readFileSync('new_rules.txt', 'utf8')); fs.writeFileSync('src/lib/prompts.ts', extractLines.join('
')); console.log('Rules perfectly updated!'); } else { console.log('Indices not found!', startIdx, endIdx); }